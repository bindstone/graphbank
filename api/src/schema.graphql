type File {
    filename: String!
    mimetype: String!
    encoding: String!
}

type Currency {
    id: ID!
    iso: String
    description: String
    exchange: Float
}

type Client {
    id: ID!
    firstName: String
    lastName: String
    currency: Currency @relation(name: "BASED", direction: "BOTH")
}
